---
phase: 02-shell-chaining-obfuscation-interception
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [tools/terminal/safety.py, tests/test_terminal_safety_structural.py]
autonomous: true
requirements: [SAFE-01, SAFE-02]
user_setup: []

must_haves:
  truths:
    - "Unquoted shell chaining operators like ;, &&, ||, |, $(), and backticks are blocked"
    - "Chaining operators inside single/double quotes are allowed"
    - "Unquoted environment variables are contextually blocked in command verb and execution wrapper parameter positions"
    - "Caret, backslash, and backtick escapes inside words are detected and blocked as obfuscation"
  artifacts:
    - path: "tools/terminal/safety.py"
      provides: "Quote-aware chaining checks, escape checking, and contextual variable verification"
---

<objective>
Implement shell chaining detection, contextual variable lookup checks, and caret/backslash/backtick escape obfuscation detection.

Purpose: Defeat bypasses using cmdline separators and obfuscation tricks while avoiding false positives on benign commands.
Output: Refactored tools/terminal/safety.py and updated tests/test_terminal_safety_structural.py.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shell-chaining-obfuscation-interception/02-CONTEXT.md
@tools/terminal/safety.py
@tests/test_terminal_safety_structural.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement pre-tokenization quote-aware chaining scanner</name>
  <files>tools/terminal/safety.py</files>
  <read_first>tools/terminal/safety.py</read_first>
  <action>
    Add helper method `_has_chaining_operators(self, command: str) -> tuple[bool, str]` in `SafetyMixin`:
    1. Loop through raw command string.
    2. Maintain `in_single` and `in_double` quote status flags.
    3. If not in quotes, check for chaining chars: `;`, `|`, `&`, `` ` ``.
    4. Also check for `$(` outside quotes.
    5. If found, return `(True, char)` representing the matched operator.
    6. Hook this check at the very beginning of `_blocked_command_reason` after checking validation rules, returning: `Command blocked by safety rules: shell chaining operator detected ({operator})`.
  </action>
  <verify>venv\Scripts\pytest tests/test_terminal_safety_structural.py</verify>
  <acceptance_criteria>
    - `safety.py` contains `_has_chaining_operators`
    - Semicolons, pipes, and ampersands outside quotes are intercepted.
  </acceptance_criteria>
  <done>Pre-tokenization chaining operator check is implemented and active in safety.py</done>
</task>

<task type="auto">
  <name>Task 2: Implement contextual variable expansion protection</name>
  <files>tools/terminal/safety.py</files>
  <read_first>tools/terminal/safety.py</read_first>
  <action>
    Modify `SafetyMixin._detect_obfuscation` or token iteration to handle environment variables and parameter expansions:
    1. Add regex patterns matching `%VAR%` (Windows CMD), `$VAR` / `${VAR}` / `$env:VAR` (Bash/PowerShell).
    2. Block if a variable pattern is detected in the first token (command verb position).
    3. Block if a variable pattern is detected inside execution wrapper parameters (e.g. following `-Command` or `/c`).
    4. Return block reason: `Command blocked by safety rules: environment variable expansion detected in verb position` or similar.
  </action>
  <verify>venv\Scripts\pytest tests/test_terminal_safety_structural.py</verify>
  <acceptance_criteria>
    - `safety.py` contextually blocks variables.
  </acceptance_criteria>
  <done>Contextual variable expansion block is implemented in safety.py</done>
</task>

<task type="auto">
  <name>Task 3: Implement caret, backslash, and backtick escape obfuscation check</name>
  <files>tools/terminal/safety.py</files>
  <read_first>tools/terminal/safety.py</read_first>
  <action>
    Extend `SafetyMixin._detect_obfuscation(token)` and `SafetyMixin._clean_token(token)`:
    1. Track if a token contains caret `^`, backslash `\`, or backtick `` ` `` characters.
    2. Add escape cleansing. Strip all these escape characters along with quotes to produce the raw word.
    3. If the token contained escape characters, and its raw cleaned form is a simple word (matches `^[a-zA-Z0-9_-]+$`), flag it as suspicious obfuscation and return: `Command blocked by safety rules: command obfuscation detected (escape characters in token: {token})`.
  </action>
  <verify>venv\Scripts\pytest tests/test_terminal_safety_structural.py</verify>
  <acceptance_criteria>
    - `safety.py` blocks escape-character word obfuscation.
  </acceptance_criteria>
  <done>Escape obfuscation checker is implemented in safety.py</done>
</task>

<task type="auto">
  <name>Task 4: Add unit tests for chaining, variables, and escapes</name>
  <files>tests/test_terminal_safety_structural.py</files>
  <read_first>tests/test_terminal_safety_structural.py</read_first>
  <action>
    Add test cases to `tests/test_terminal_safety_structural.py` covering:
    1. Chaining operators:
       - `echo hello && sc stop` (blocked)
       - `echo hello; sc stop` (blocked)
       - `echo hello|sc stop` (blocked)
       - `echo $(whoami)` (blocked)
       - ``echo `whoami``` (blocked)
       - `echo "hello && welcome"` (allowed)
       - `echo 'hello; world'` (allowed)
    2. Variable expansions:
       - `%COMSPEC% /c sc` (blocked)
       - `powershell -c $a` (blocked)
       - `echo $PATH` (allowed)
       - `echo %USERNAME%` (allowed)
    3. Escape obfuscation:
       - `n^e^t stop` (blocked)
       - `s\c query` (blocked)
       - `n`e`t start` (blocked)
       - `C:\Windows\System32\sc.exe` (allowed)
  </action>
  <verify>venv\Scripts\pytest tests/test_terminal_safety_structural.py</verify>
  <acceptance_criteria>
    - All tests in `tests/test_terminal_safety_structural.py` pass without errors.
  </acceptance_criteria>
  <done>Tests for chaining, variables, and escapes are added and pass successfully</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `venv\Scripts\pytest tests/test_terminal_safety_structural.py`
- [ ] Run `venv\Scripts\pytest tests/test_validators.py`
</verification>

<success_criteria>
- All tasks completed
- All verification tests pass
- No errors or warnings introduced
- 100% test coverage of new chaining and obfuscation logic
</success_criteria>

<output>
After completion, create `.planning/phases/02-shell-chaining-obfuscation-interception/02-01-SUMMARY.md`
</output>
