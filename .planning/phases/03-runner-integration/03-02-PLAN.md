---
phase: "03"
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [tests/test_terminal_safety_structural.py]
autonomous: true
requirements: [TEST-01]
user_setup: []
---

# Plan 03-02: Safety Integration Verification & Performance Testing

## Objective
Write comprehensive unit and integration tests to verify script validation, error formatting, and security audit log generation, and benchmark performance to ensure it meets constraints.

## Tasks

<task type="auto">
  <name>Task 1: Add tests for script validation, error formatting, and audit logs</name>
  <files>tests/test_terminal_safety_structural.py</files>
  <read_first>tests/test_terminal_safety_structural.py</read_first>
  <action>
    Add test cases to `tests/test_terminal_safety_structural.py`:
    - Test `run_script` with temporary scripts created via pytest's `tmp_path` fixture (e.g., .sh, .ps1, .bat).
    - Assert that scripts containing blocked commands are intercepted and return the normalized error string.
    - Assert that benign commands and script comment lines / blank lines are correctly skipped and allowed to run.
    - Assert that line continuation merges (using `\`, `` ` ``, `^`) are correctly reconstructed before validation.
    - Test that blocked execution triggers security warning logging and records a structured audit error under `security_validation`.
  </action>
  <verify>.\\venv\\Scripts\\pytest tests/test_terminal_safety_structural.py</verify>
  <acceptance_criteria>
    - All new script validation, error formatting, and logging integration tests pass successfully.
  </acceptance_criteria>
  <done>Comprehensive verification tests are added and pass.</done>
</task>

<task type="auto">
  <name>Task 2: Benchmark and verify safety validation performance is &lt;10ms</name>
  <files>tests/test_terminal_safety_structural.py</files>
  <read_first>tests/test_terminal_safety_structural.py</read_first>
  <action>
    Add a benchmark performance test in `tests/test_terminal_safety_structural.py`.
    Measure the latency of `_blocked_command_reason` over 100+ iterations using a set of complex, nested, and obfuscated commands.
    Assert that the average validation time per command is strictly less than 10 milliseconds.
  </action>
  <verify>.\\venv\\Scripts\\pytest tests/test_terminal_safety_structural.py -k test_safety_validation_performance</verify>
  <acceptance_criteria>
    - Performance test asserts validation latency is strictly under 10ms.
    - Test runs and passes as part of the test suite.
  </acceptance_criteria>
  <done>Performance benchmarking test is implemented and verified.</done>
</task>

## Verification
```bash
.\\venv\\Scripts\\pytest tests/test_terminal_safety_structural.py
```

## Success Criteria
- Safety validation behaves correctly and returns expected outputs for all types of scripts.
- Obfuscated commands, chaining operators, comments, and continuation lines are correctly resolved and validated within scripts.
- Execution speed is verified to be well below the 10ms threshold.

## Must Haves
- Average validation latency must be strictly less than 10ms.
- 100% pass rate for newly added script validation, logging, and performance tests.
