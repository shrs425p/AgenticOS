---
phase: 01-core-security-and-code-quality-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["core/guardrails.py", "tests/test_safety.py"]
autonomous: true
requirements: ["SEC-04"]

must_haves:
  truths:
    - "Symlink traversal depth is tracked and strictly limited to 5 resolved links"
    - "exceeding the depth limit triggers a ValueError and blocks path access"
  artifacts:
    - path: "core/guardrails.py"
      provides: "Recursive symlink depth resolver integrated in PathGuard"
  key_links:
    - from: "tests/test_safety.py"
      to: "core/guardrails.py"
      via: "imports PathGuard and verifies check_path blocks traversal depth exceeding 5"
---

<objective>
Implement recursive symlink traversal depth validation within PathGuard.

Purpose: Prevent path traversal exploits utilizing chained symlinks/junctions to reference sensitive files outside the workspace boundary.
Output: Recursive symlink validator in PathGuard, integration tests in tests/test_safety.py.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-security-and-code-quality-foundation/01-CONTEXT.md
@.planning/phases/01-core-security-and-code-quality-foundation/01-RESEARCH.md
@.planning/phases/01-core-security-and-code-quality-foundation/01-VALIDATION.md
@core/guardrails.py
</context>

<threat_model>
  <trust_boundaries>
    - File system path validation boundary (PathGuard).
  </trust_boundaries>
  <stride_register>
    - **Spoofing**: N/A for this plan.
    - **Tampering (T-01-03 / Symlink Traversal)**: Attackers bypass directory containment checks using symlinks to link to files outside the workspace. Resolved by step-by-step symlink resolution limiting traversal depth to 5.
    - **Repudiation**: N/A for this plan.
    - **Information Disclosure**: Unauthorized file extraction via traversal. Resolved by PathGuard containment check.
    - **Denial of Service**: Infinite loops or deep recursive folders causing filesystem stalls. Mitigated by depth cutoff.
    - **Elevation of Privilege**: N/A for this plan.
  </stride_register>
</threat_model>

## Artifacts this phase produces
- `core/guardrails.py`: updated with `resolve_with_symlink_depth` helper and integrated within `PathGuard.check_path`.

<tasks>
<task type="auto">
  <name>Task 1: Implement symlink depth resolver in PathGuard</name>
  <files>core/guardrails.py</files>
  <read_first>core/guardrails.py, .planning/phases/01-core-security-and-code-quality-foundation/01-RESEARCH.md</read_first>
  <action>
    Add a helper function `resolve_with_symlink_depth(path: Path, max_depth: int = 5) -> Path` to `core/guardrails.py`.
    The helper function must walk path components, resolve symlinks recursively using `os.readlink()`, count resolved links, and raise `ValueError` if the count exceeds `max_depth` (5).
    Integrate this helper inside `PathGuard.check_path` to replace standard `.resolve()` calls.
  </action>
  <verify>Run `pytest tests/test_safety.py -k test_symlink_depth`</verify>
  <done>Symlink depth validation function is fully integrated within PathGuard path checks.</done>
</task>

<task type="auto">
  <name>Task 2: Add symlink depth tests in tests/test_safety.py</name>
  <files>tests/test_safety.py</files>
  <read_first>core/guardrails.py, tests/test_safety.py</read_first>
  <action>
    Extend `tests/test_safety.py` to add test cases for path canonicalization and symlink containment:
      - Valid paths resolve correctly.
      - Symlink resolution within limit (<= 5 links) succeeds.
      - Nested symlink chain resolution exceeding limit (> 5 links) is blocked and throws ValueError/PathGuard violation.
  </action>
  <verify>Run `pytest tests/test_safety.py -k test_symlink`</verify>
  <done>Unit tests verifying symlink depth constraints are written and passing.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest tests/test_safety.py -k test_symlink passes
</verification>

<success_criteria>
- All tasks completed.
- PathGuard tracks symlink depth and blocks traversal when depth exceeds 5.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-security-and-code-quality-foundation/01-02-SUMMARY.md`
</output>
