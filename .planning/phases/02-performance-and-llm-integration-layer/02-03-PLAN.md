---
phase: 02-performance-and-llm-integration-layer
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: ["core/model_clients.py", "tests/test_fallback_router.py"]
autonomous: true
requirements: ["INTEG-01", "INTEG-02", "INTEG-03", "INTEG-04", "INTEG-05", "DOC-03"]
---

# Plan 02-03: Resilient Fallback Routing & Token Budgets

<objective>
Implement cost-aware model fallback routing cascading on transient errors and pre-flight token calculators alerting on budget limits.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-performance-and-llm-integration-layer/02-CONTEXT.md
@.planning/phases/02-performance-and-llm-integration-layer/02-RESEARCH.md
</context>

<threat_model>
## Trust Boundaries
- Outer remote client API communication boundaries.

## STRIDE Register
| ID | Threat | Component | Mitigation |
|----|--------|-----------|------------|
| T-02-05 | Information Disclosure | model_clients.py | Ensure fallback client configurations do not print auth keys or authorization headers into stderr logs. |
| T-02-06 | Elevation of Privilege | model_clients.py | Route fallback queries to models that maintain equivalent or higher security policies. |
</threat_model>

## Artifacts this phase produces
- `core/model_clients.py`: updated with FallbackRouter and TokenBudgetChecker.
- `tests/test_fallback_router.py`: suite verifying model routing transitions.

<tasks>
  <task id="02-03-01" type="implementation" wave="3" depends_on="[]">
    <read_first>core/model_clients.py</read_first>
    <action>
      Implement `FallbackRouter` class and `build_fallback_router` factory in `core/model_clients.py`:
      - Router intercepts rate-limits, auth failures, and context window errors.
      - Wait/throttles on rate limit; upgrades to next-tier model on context overflow; skips immediately on key auth failure.
    </action>
    <verify>Dispatch mock model failures to verify fallback transitions.</verify>
    <done>FallbackRouter is implemented and integrated.</done>
  </task>

  <task id="02-03-02" type="implementation" wave="3" depends_on="[]">
    <read_first>core/model_clients.py</read_first>
    <action>
      Implement `TokenBudgetChecker` class in `core/model_clients.py`:
      - Estimates total message tokens using character-to-token heuristics.
      - Emits warnings if tokens exceed 85% warn threshold, or blocks if limit exceeded.
    </action>
    <verify>Validate token budgets under low and high token scenarios.</verify>
    <done>TokenBudgetChecker pre-flight checks are implemented.</done>
  </task>

  <task id="02-03-03" type="implementation" wave="3" depends_on="[]">
    <read_first>tests/test_fallback_router.py</read_first>
    <action>
      Create unit tests in `tests/test_fallback_router.py` verifying router fallback cascades, throttling behaviors, auth-fail skips, and budget checker warnings.
    </action>
    <verify>Run `pytest tests/test_fallback_router.py` and confirm all tests pass.</verify>
    <done>Router and budget test suite runs clean.</done>
  </task>
</tasks>

<wave_summary>
| Wave | Tasks | Parallel? | Notes |
|------|-------|-----------|-------|
| 3 | 02-03-01 through 02-03-03 | Yes | Code model updates and tests files can be modified concurrently. |
</wave_summary>

<success_criteria>
- Router cascades to backup models on rate-limit, auth, or context errors.
- Pre-flight budget checker warns when near token limits.
- All test suites execute successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/02-performance-and-llm-integration-layer/02-03-SUMMARY.md`
</output>
