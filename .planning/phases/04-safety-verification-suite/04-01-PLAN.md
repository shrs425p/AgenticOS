---
phase: "04"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [tools/terminal/safety.py, tools/terminal/runner.py, core/runtime.py, tests/test_terminal_safety_structural.py, tests/test_terminal_safety_integration.py]
autonomous: true
requirements: [TEST-01]
user_setup: []
---

<objective>
Conduct a comprehensive verification audit with test suites verifying all safety gates (functional requirement TEST-01) and resolve the security bypasses and logging issues identified in 03-REVIEW.md (CR-01, WR-01, WR-02, and Info-level findings).
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Resolve CR-01, WR-01, WR-02, and Info-level codebase findings</name>
  <files>tools/terminal/safety.py, tools/terminal/runner.py, core/runtime.py</files>
  <read_first>tools/terminal/safety.py, tools/terminal/runner.py, core/runtime.py</read_first>
  <action>
    1. In tools/terminal/safety.py:
       - Implement '_is_powershell_command_flag(self, token: str) -> bool' to match PowerShell command parameters (prefix of 'command' ignoring leading hyphen/slash).
       - Implement '_is_powershell_encoded_flag(self, token: str) -> bool' to match PowerShell base64 encoded parameters (prefix of 'encodedcommand' ignoring leading hyphen/slash).
       - Modify '_blocked_command_reason' to match wrapper flags using the prefix matching methods.
       - Implement base64 decoding of UTF-16LE strings for matched encoded flags. Pass the decoded payload recursively back through '_blocked_command_reason'. Block with a base64-decode-failure warning if decoding fails.
    2. In tools/terminal/runner.py:
       - Add '.zsh: "\\"' to the '_CONTINUATION_CHARS' dictionary.
       - Include '.zsh' in the script comment filtering logic inside '_validate_script_content' so comment lines are correctly skipped.
       - Extract duplicated self-healing logic inside '_run' and the 'FileNotFoundError' catch block into a single private helper '_attempt_self_healing'.
       - Update docstrings for public run tools 'run_command', 'run_powershell', and 'run_python' to follow Google-style formatting.
    3. In core/runtime.py:
       - Declare 'ok = False' and 'obs_text = ""' in the outer scope before the tool audit logging try/except block to resolve variable scope risks.
       - Sort standard library and local module imports alphabetically to satisfy codebase conventions.
  </action>
  <verify>pytest tests/test_terminal_safety_structural.py</verify>
  <acceptance_criteria>
    - Python linter and formatter (ruff check/black) return no errors.
    - Existing unit tests in tests/test_terminal_safety_structural.py pass.
    - No UnboundLocalError is encountered in core/runtime.py when tool audit logging fails.
    - Zsh scripts are validated line-by-line using line continuation and comment filters.
  </acceptance_criteria>
  <done>Codebase security vulnerabilities, script checks, and logging variable scope issues are resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Build comprehensive unit and integration test suites</name>
  <files>tests/test_terminal_safety_structural.py, tests/test_terminal_safety_integration.py</files>
  <read_first>tests/test_terminal_safety_structural.py</read_first>
  <action>
    1. In tests/test_terminal_safety_structural.py, add new unit tests covering:
       - PowerShell flag abbreviations (e.g., '-comm', '-co').
       - PowerShell case-insensitive flags (e.g., '-CoM').
       - PowerShell Base64 encoded blocked commands (e.g., 'sc stop spooler' encoded in base64 UTF-16LE).
       - PowerShell Base64 with variable expansion or invalid base64 payloads.
       - Zsh script line continuation validation and comment skipping.
       - Shell chaining operators (subshells, backticks).
       - Command verb obfuscation (ticks, slashes, carets, nested quotes).
       - Environment variable expansions in verb and wrapper parameters.
    2. Create tests/test_terminal_safety_integration.py to define integration tests running live host-OS execution. Use pytest.mark.skipif to bypass integration checks if powershell or bash is not available on the running host OS. Verify that calling 'run_command' or 'run_powershell' with blocked commands returns the correct 'Error: Command blocked by safety rules:' format.
  </action>
  <verify>pytest tests/test_terminal_safety_structural.py tests/test_terminal_safety_integration.py</verify>
  <acceptance_criteria>
    - Automated test suite runs and passes cleanly.
    - New tests assert the full matrix of command injection and obfuscation techniques.
    - Integration tests run live shells on host OS and return safety validation blocks.
  </acceptance_criteria>
  <done>Comprehensive unit and integration test suites are written and verified successfully.</done>
</task>
</tasks>

<verification>
pytest tests/test_terminal_safety_structural.py tests/test_terminal_safety_integration.py
</verification>

<success_criteria>
- All security bypass vectors (abbreviated parameters, base64 payloads) are blocked.
- Script validation covers Zsh shell scripts (.zsh).
- Auditing log scope bug is fixed.
- 100% of the safety verification suite runs and passes cleanly.
</success_criteria>

<must_haves>
- D-01: Resolve all code review findings (CR-01 PowerShell bypass, WR-01 Zsh support, WR-02 log scope error).
- D-02: Implement hybrid test suite with mocks and live host integration tests.
- D-03: Cover the full attack matrix (chaining, obfuscation, PowerShell, env variables, script injections).
- Average command safety validation time must remain under 10ms.
- Blocks on shell execution must return normalized format: 'Error: Command blocked by safety rules: [reason]'.
- PowerShell parameters prefix-matching matches any unique prefix of 'command' and 'encodedcommand'.
- UTF-16LE decoding is utilized for PowerShell EncodedCommand strings.
</must_haves>

## Artifacts this phase produces

### Symbols Created
- `SafetyMixin._is_powershell_command_flag` (method in `tools/terminal/safety.py`)
- `SafetyMixin._is_powershell_encoded_flag` (method in `tools/terminal/safety.py`)
- `RunnerMixin._attempt_self_healing` (method in `tools/terminal/runner.py`)
- `tests/test_terminal_safety_integration.py` (file containing live subprocess execution tests)

### Symbols Modified
- `SafetyMixin._blocked_command_reason` (method in `tools/terminal/safety.py`)
- `RunnerMixin._CONTINUATION_CHARS` (dict in `tools/terminal/runner.py`)
- `RunnerMixin._validate_script_content` (method in `tools/terminal/runner.py`)
- `RunnerMixin.run_command` (docstring update in `tools/terminal/runner.py`)
- `RunnerMixin.run_powershell` (docstring update in `tools/terminal/runner.py`)
- `RunnerMixin.run_python` (docstring update in `tools/terminal/runner.py`)
- `AgentRuntime.run` (variables and imports inside `core/runtime.py`)
- `tests/test_terminal_safety_structural.py` (added unit tests)
