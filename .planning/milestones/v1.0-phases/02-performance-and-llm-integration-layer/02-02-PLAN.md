---
phase: 02-performance-and-llm-integration-layer
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: ["kernel/dispatch.py", "kernel/discovery.py", "spec/dispatcherparallelspec.py", "spec/tooldiscoveryspec.py"]
autonomous: true
requirements: ["PERF-02", "PERF-03"]
---

# Plan 02-02: Streaming Parser & Parallel Scheduler

<objective>
Implement dynamic JSON streaming action parser and parallel scheduler resolving tool graph dependencies to execute actions concurrently.
</objective>

<execution_context>
@.agents/gsd-kernel/workflows/execute-plan.md
@.agents/gsd-kernel/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-performance-and-llm-integration-layer/02-CONTEXT.md
@.planning/phases/02-performance-and-llm-integration-layer/02-RESEARCH.md
</context>

<threat_model>
## Trust Boundaries
- Concurrency workers boundary dispatching OS-level commands concurrently.

## STRIDE Register
| ID | Threat | Component | Mitigation |
|----|--------|-----------|------------|
| T-02-03 | Denial of Service | dispatcher.py | ThreadPoolExecutor must cap maximum worker concurrency count (default to 4) to prevent exhaustion of system handles. |
| T-02-04 | Tampering | dispatcher.py | Dependency waves topological sort must raise cyclic dependency errors if a task graph cycle is detected. |
</threat_model>

## Artifacts this phase produces
- `kernel/dispatch.py`: updated with StreamingActionParser and ParallelScheduler.
- `kernel/discovery.py`: SemanticToolIndex for TF-IDF cosine-similarity tool discovery.
- `spec/dispatcherparallelspec.py`: spec verifying parallel graphs and streaming actions.
- `spec/tooldiscoveryspec.py`: spec verifying similarity search results.

<tasks>
  <task id="02-02-01" type="implementation" wave="2" depends_on="[]">
    <read_first>kernel/dispatch.py</read_first>
    <action>
      Implement `StreamingActionParser` class inside `kernel/dispatch.py` providing `.feed()`, `.flush()`, and `.reset()` methods to yield parsed JSON blocks from streaming text chunks.
    </action>
    <verify>Feed partial JSON blocks to verify they are parsed and yielded correctly.</verify>
    <done>StreamingActionParser is fully implemented.</done>
  </task>

  <task id="02-02-02" type="implementation" wave="2" depends_on="[]">
    <read_first>kernel/dispatch.py</read_first>
    <action>
      Implement `ParallelScheduler` class and `execute_actions_parallel` function in `kernel/dispatch.py`:
      - Group actions into parallel execution waves using Kahn's topological sort.
      - Dispatch independent actions in parallel via ThreadPoolExecutor.
      - Fallback to sequential run if only 1 action exists or parallel execution is disabled.
    </action>
    <verify>Validate waves grouping and concurrency thread speeds in spec.</verify>
    <done>ParallelScheduler execution logic is complete.</done>
  </task>

  <task id="02-02-03" type="implementation" wave="2" depends_on="[]">
    <read_first>kernel/discovery.py</read_first>
    <action>
      Implement `SemanticToolIndex` inside `kernel/discovery.py` using TF-IDF token weighting and cosine similarity queries to return top-k matching tool records.
    </action>
    <verify>Query mock indexes with natural descriptions and verify correct ops return.</verify>
    <done>SemanticToolIndex discovery is implemented.</done>
  </task>

  <task id="02-02-04" type="implementation" wave="2" depends_on="[]">
    <read_first>spec/dispatcherparallelspec.py</read_first>
    <action>
      Create unit spec in `spec/dispatcherparallelspec.py` and `spec/tooldiscoveryspec.py` covering parser streaming, scheduler dependency waves, sequential fallbacks, and semantic index searches.
    </action>
    <verify>Run pytest spec/dispatcherparallelspec.py spec/tooldiscoveryspec.py and verify all spec pass.</verify>
    <done>concurrency and discovery spec pass cleanly.</done>
  </task>
</tasks>

<wave_summary>
| Wave | Tasks | Parallel? | Notes |
|------|-------|-----------|-------|
| 2 | 02-02-01 through 02-02-03 | Yes | Core dispatcher and discovery modules can be written concurrently. |
| 2 | 02-02-04 | No | Tests require completion of the components. |
</wave_summary>

<success_criteria>
- Streaming action blocks are parsed incrementally.
- Concurrent scheduler sorts action dependency waves.
- Semantic TF-IDF search resolves relevant tool records.
- All test suites run successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/02-performance-and-llm-integration-layer/02-02-SUMMARY.md`
</output>
