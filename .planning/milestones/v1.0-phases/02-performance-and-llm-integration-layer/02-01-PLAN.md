---
phase: 02-performance-and-llm-integration-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["core/context_engine.py", "tests/test_context_engine.py"]
autonomous: true
requirements: ["PERF-01", "PERF-04"]
---

# Plan 02-01: Adaptive Context Compaction & Truncation

<objective>
Implement middle-truncation for large observation outputs and history compaction managers to optimize LLM context window utilization and prevent context overflows.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-performance-and-llm-integration-layer/02-CONTEXT.md
@.planning/phases/02-performance-and-llm-integration-layer/02-RESEARCH.md
</context>

<threat_model>
## Trust Boundaries
- Conversation history buffers managed in active memory.

## STRIDE Register
| ID | Threat | Component | Mitigation |
|----|--------|-----------|------------|
| T-02-01 | Information Disclosure | context_engine.py | Ensure history compaction summaries do not leak hidden credential keys or internal environment properties. |
| T-02-02 | Denial of Service | context_engine.py | Mitigate window crash risks by implementing robust string length checks and fallback log prunings. |
</threat_model>

## Artifacts this phase produces
- `core/context_engine.py`: updated with large message middle-collapsing and sliding window history compaction.
- `tests/test_context_engine.py`: unit test coverage validating compaction states.

<tasks>
  <task id="02-01-01" type="implementation" wave="1" depends_on="[]">
    <read_first>core/context_engine.py</read_first>
    <action>
      Implement `collapse_large_messages(messages: list) -> list` inside `ContextEngine`:
      - Iterate through conversation messages. If a message's content is an observation or command log exceeding 4000 characters, collapse the middle portion with `[... COLLAPSED X CHARACTERS ...]` keeping the first 2000 and last 2000 characters intact.
    </action>
    <verify>Validate collapsing on large mock messages in tests.</verify>
    <done>Large observation text collapsing is implemented.</done>
  </task>

  <task id="02-01-02" type="implementation" wave="1" depends_on="[]">
    <read_first>core/context_engine.py</read_first>
    <action>
      Implement sliding-window compaction `compact_history(messages: list) -> list` in `ContextEngine`:
      - Calculate message history tokens.
      - If history exceeds thresholds, summarize older message history logs using LLM chat clients.
      - On API failure, fall back to default text pruning and inject truncation notices.
    </action>
    <verify>Verify token calculations and compaction fallbacks under mock failures.</verify>
    <done>compaction manager with API error fallback is implemented.</done>
  </task>

  <task id="02-01-03" type="implementation" wave="1" depends_on="[]">
    <read_first>tests/test_context_engine.py</read_first>
    <action>
      Create unit tests in `tests/test_context_engine.py` covering message collapsing, token estimation limits, sliding window compaction, and API failure fallbacks.
    </action>
    <verify>Run `pytest tests/test_context_engine.py` and verify all tests pass.</verify>
    <done>Unit tests pass cleanly.</done>
  </task>
</tasks>

<wave_summary>
| Wave | Tasks | Parallel? | Notes |
|------|-------|-----------|-------|
| 1 | 02-01-01 through 02-01-03 | Yes | Core context engine and test files can be modified in parallel. |
</wave_summary>

<success_criteria>
- Middle-collapsing preserves first and last 2000 characters of large text messages.
- sliding-window compactor collapses logs when context sizes are reached.
- Unit tests run and pass without regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-performance-and-llm-integration-layer/02-01-SUMMARY.md`
</output>
