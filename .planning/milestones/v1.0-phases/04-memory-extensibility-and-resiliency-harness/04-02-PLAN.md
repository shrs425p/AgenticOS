---
phase: 04-memory-extensibility-and-resiliency-harness
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - core/tool_base.py
  - core/dispatcher.py
  - tests/test_async_tools.py
autonomous: true
requirements:
  - EXT-01
  - EXT-02
  - EXT-05
must_haves:
  truths:
    - "The AsyncTool protocol defines an async __call__ coroutine and an async generator stream method."
    - "The ParallelScheduler can run AsyncTool instances concurrently using asyncio.gather within a task wave."
    - "The system supports piping output streams from one tool directly to another."
  artifacts:
    - "core/tool_base.py"
    - "core/dispatcher.py"
    - "tests/test_async_tools.py"
  key_links:
    - "ParallelScheduler in core/dispatcher.py executes both synchronous threads and async loop tasks concurrently in waves."
---

<objective>
Implement AsyncTool protocols in core/tool_base.py, extend the parallel dispatcher (core/dispatcher.py) to support scheduling and running async tools concurrently via asyncio, and add output streaming piping composers.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@core/tool_base.py
@core/dispatcher.py
</context>

<tasks>

<task type="auto">
  <name>Task 04-02-01: Update Tool Base with AsyncTool Protocol</name>
  <files>core/tool_base.py</files>
  <read_first>core/tool_base.py</read_first>
  <action>
    - Add `AsyncTool` protocol class to `core/tool_base.py` which defines `async def __call__(self, *args, **kwargs)` and `async def stream(self, *args, **kwargs) -> AsyncIterator[str]`.
    - Update `@tool` decorator to set `func._is_async = asyncio.iscoroutinefunction(func)`.
  </action>
  <verify>Verify core/tool_base.py syntax and import availability.</verify>
  <acceptance_criteria>
    - `AsyncTool` is importable and `@tool` correctly detects async coroutine functions.
  </acceptance_criteria>
  <done>AsyncTool protocol and decorator checks are implemented.</done>
</task>

<task type="auto">
  <name>Task 04-02-02: Support Async Execution in ParallelScheduler</name>
  <files>core/dispatcher.py</files>
  <read_first>core/dispatcher.py</read_first>
  <action>
    - Update `ParallelScheduler.execute()` and `_safe_call` to check if target tool is async.
    - Inside waves, group async tools and execute them concurrently via `asyncio.run()` (or run them inside the current running event loop using `asyncio.create_task` or helper thread-safe methods) while executing sync tools concurrently on threads.
  </action>
  <verify>Check core/dispatcher.py for imports of asyncio and handling of coroutine functions.</verify>
  <acceptance_criteria>
    - `ParallelScheduler` executes async tools concurrently alongside sync ones.
  </acceptance_criteria>
  <done>Parallel scheduler supports concurrent async tool execution.</done>
</task>

<task type="auto">
  <name>Task 04-02-03: Implement Output Streaming & Piping Composers</name>
  <files>core/dispatcher.py</files>
  <read_first>core/dispatcher.py</read_first>
  <action>
    - Implement a decorator or composer function `pipe_tools(tool_a, tool_b)` to chain tools together: outputs of `tool_a` (streaming chunks) are passed as inputs to `tool_b`.
    - Expose this composer to tool execution chains.
  </action>
  <verify>Verify presence of pipe_tools function.</verify>
  <acceptance_criteria>
    - `pipe_tools` is implemented and exports correctly.
  </acceptance_criteria>
  <done>Piping composer logic is fully implemented.</done>
</task>

<task type="auto">
  <name>Task 04-02-04: Implement Async Tool Unit Tests</name>
  <files>tests/test_async_tools.py</files>
  <read_first>core/dispatcher.py</read_first>
  <action>
    Create unit tests in `tests/test_async_tools.py` verifying:
    - Decorator detects async coroutine functions.
    - Async tools execute concurrently inside `ParallelScheduler`.
    - Async iterator streaming of output chunks works correctly.
    - `pipe_tools` streams and runs chained executions.
  </action>
  <verify>Run `pytest tests/test_async_tools.py`</verify>
  <acceptance_criteria>
    - All async execution tests pass.
  </acceptance_criteria>
  <done>Unit tests for async tool execution and piping are complete and verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run tests: `venv\Scripts\pytest tests/test_async_tools.py`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/04-memory-extensibility-and-resiliency-harness/04-02-SUMMARY.md`
</output>
