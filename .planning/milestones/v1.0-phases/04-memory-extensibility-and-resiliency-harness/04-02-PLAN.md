---
phase: 04-memory-extensibility-and-resiliency-harness
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - kernel/base.py
  - kernel/dispatch.py
  - spec/test_async_ops.py
autonomous: true
requirements:
  - EXT-01
  - EXT-02
  - EXT-05
must_haves:
  truths:
    - "The AsyncTool protocol defines an async __call__ coroutine and an async generator stream method."
    - "The ParallelScheduler can run AsyncTool instances concurrently using asyncio.gather within a task wave."
    - "The system supports piping output streams from one tool directly to another."
  artifacts:
    - "kernel/base.py"
    - "kernel/dispatch.py"
    - "spec/test_async_ops.py"
  key_links:
    - "ParallelScheduler in kernel/dispatch.py executes both synchronous threads and async loop tasks concurrently in waves."
---

<objective>
Implement AsyncTool protocols in kernel/base.py, extend the parallel dispatcher (kernel/dispatch.py) to support scheduling and running async ops concurrently via asyncio, and add output streaming piping composers.
</objective>

<execution_context>
@.agents/gsd-kernel/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@kernel/base.py
@kernel/dispatch.py
</context>

<tasks>

<task type="auto">
  <name>Task 04-02-01: Update Tool Base with AsyncTool Protocol</name>
  <files>kernel/base.py</files>
  <read_first>kernel/base.py</read_first>
  <action>
    - Add `AsyncTool` protocol class to `kernel/base.py` which defines `async def __call__(self, *args, **kwargs)` and `async def stream(self, *args, **kwargs) -> AsyncIterator[str]`.
    - Update `@tool` decorator to set `func._is_async = asyncio.iscoroutinefunction(func)`.
  </action>
  <verify>Verify kernel/base.py syntax and import availability.</verify>
  <acceptance_criteria>
    - `AsyncTool` is importable and `@tool` correctly detects async coroutine functions.
  </acceptance_criteria>
  <done>AsyncTool protocol and decorator checks are implemented.</done>
</task>

<task type="auto">
  <name>Task 04-02-02: Support Async Execution in ParallelScheduler</name>
  <files>kernel/dispatch.py</files>
  <read_first>kernel/dispatch.py</read_first>
  <action>
    - Update `ParallelScheduler.execute()` and `_safe_call` to check if target tool is async.
    - Inside waves, group async ops and execute them concurrently via `asyncio.run()` (or run them inside the current running event loop using `asyncio.create_task` or helper thread-safe methods) while executing sync ops concurrently on threads.
  </action>
  <verify>Check kernel/dispatch.py for imports of asyncio and handling of coroutine functions.</verify>
  <acceptance_criteria>
    - `ParallelScheduler` executes async ops concurrently alongside sync ones.
  </acceptance_criteria>
  <done>Parallel scheduler supports concurrent async tool execution.</done>
</task>

<task type="auto">
  <name>Task 04-02-03: Implement Output Streaming & Piping Composers</name>
  <files>kernel/dispatch.py</files>
  <read_first>kernel/dispatch.py</read_first>
  <action>
    - Implement a decorator or composer function `pipe_ops(tool_a, toolb)` to chain ops together: outputs of `tool_a` (streaming chunks) are passed as inputs to `toolb`.
    - Expose this composer to tool execution chains.
  </action>
  <verify>Verify presence of pipe_ops function.</verify>
  <acceptance_criteria>
    - `pipe_ops` is implemented and exports correctly.
  </acceptance_criteria>
  <done>Piping composer logic is fully implemented.</done>
</task>

<task type="auto">
  <name>Task 04-02-04: Implement Async Tool Unit Tests</name>
  <files>spec/test_async_ops.py</files>
  <read_first>kernel/dispatch.py</read_first>
  <action>
    Create unit spec in `spec/test_async_ops.py` verifying:
    - Decorator detects async coroutine functions.
    - Async ops execute concurrently inside `ParallelScheduler`.
    - Async iterator streaming of output chunks works correctly.
    - `pipe_ops` streams and runs chained executions.
  </action>
  <verify>Run `pytest spec/test_async_ops.py`</verify>
  <acceptance_criteria>
    - All async execution spec pass.
  </acceptance_criteria>
  <done>Unit spec for async tool execution and piping are complete and verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run spec: `venv\Scripts\pytest spec/test_async_ops.py`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/04-memory-extensibility-and-resiliency-harness/04-02-SUMMARY.md`
</output>
