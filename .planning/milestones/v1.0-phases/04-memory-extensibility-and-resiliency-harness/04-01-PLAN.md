---
phase: 04-memory-extensibility-and-resiliency-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ops/addons/vector_memory.py
  - spec/vectormemoryspec.py
autonomous: true
requirements:
  - MEM-01
  - MEM-02
  - MEM-03
  - MEM-04
  - MEM-05
must_haves:
  truths:
    - "Retrieving a memory with evidence validation yields only records containing verified evidence fields."
    - "Vector memory searches correctly apply exponential time-decay half-life weighting."
    - "Vector search uses IVF partitioning to accelerate lookup times when dataset grows large."
    - "Episodic memory clustering groups records by numpy K-Means centroids."
  artifacts:
    - "ops/addons/vector_memory.py"
    - "spec/vectormemoryspec.py"
  key_links:
    - "VectorDB class in ops/addons/vector_memory.py uses K-Means from numpy."
---

<objective>
Refactor VectorDB to implement Inverted File (IVF) partitioning, K-Means centroids clustering, 30-day half-life exponential time-decay, and evidence field validation using numpy.
</objective>

<execution_context>
@.agents/gsd-kernel/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@ops/addons/vector_memory.py
</context>

<tasks>

<task type="auto">
  <name>Task 04-01-01: Refactor VectorDB for IVF Partitioning & K-Means</name>
  <files>ops/addons/vector_memory.py</files>
  <read_first>ops/addons/vector_memory.py</read_first>
  <action>
    - Add IVF K-Means partitioning to `VectorDB`.
    - Implement a `train_ivf()` method to compute $K$ cluster centroids using K-Means clustering in numpy when records > 10.
    - When searching, compute cosine similarity of the query against cluster centroids, identify the nearest centroid, and search only vectors within that partition.
  </action>
  <verify>Verify code syntax and class signatures in vector_memory.py</verify>
  <acceptance_criteria>
    - `VectorDB` contains a `train_ivf` method and IVF index structures.
  </acceptance_criteria>
  <done>VectorDB partition search works using K-Means centroids.</done>
</task>

<task type="auto">
  <name>Task 04-01-02: Implement Time Decay & Evidence Filtering</name>
  <files>ops/addons/vector_memory.py</files>
  <read_first>ops/addons/vector_memory.py</read_first>
  <action>
    - Record timestamps in stored memories.
    - During search, calculate similarity and apply exponential time decay: `skernel = cosine_sim * e^(-lambda * dt)` where `lambda = ln(2) / 30` (approx 0.0231) and `dt` is age in days.
    - Implement an `evidence_required` query parameter. If true, exclude records where `evidence` is empty, null, or missing.
  </action>
  <verify>Check vectormemorysearch arguments and result formats.</verify>
  <acceptance_criteria>
    - `vectormemorysearch` supports filtering by evidence and time decay.
  </acceptance_criteria>
  <done>Search decays skernels properly and validates evidence field.</done>
</task>

<task type="auto">
  <name>Task 04-01-03: Expose Episodic Clustering & Cross-Instance Sharing</name>
  <files>ops/addons/vector_memory.py</files>
  <read_first>ops/addons/vector_memory.py</read_first>
  <action>
    - Implement a tool `@tool` `clustermemories` that executes K-Means on a namespace and returns text summaries of representative centroids.
    - Support loading and saving memory DB namespace files in a shared workspace path `workspace/vectors/shared/` to support cross-instance memory sharing.
  </action>
  <verify>Ensure ops are decorated with @tool.</verify>
  <acceptance_criteria>
    - `clustermemories` is registered in the tool registry.
  </acceptance_criteria>
  <done>Episodic clustering tool is exposed and database namespaces can load/save to a shared path.</done>
</task>

<task type="auto">
  <name>Task 04-01-04: Implement Unit Tests</name>
  <files>spec/vectormemoryspec.py</files>
  <read_first>ops/addons/vector_memory.py</read_first>
  <action>
    Create detailed unit spec verifying:
    - IVF centroid calculation and nearest-centroid partition routing.
    - Cosine similarity exponential decay over time (e.g. 0 days, 30 days, 60 days).
    - Evidence verification filter logic.
    - K-Means episodic memory cluster tool outputs.
    - Cross-instance database loading and saving.
  </action>
  <verify>Run `pytest spec/vectormemoryspec.py`</verify>
  <acceptance_criteria>
    - All vector memory spec pass.
  </acceptance_criteria>
  <done>All vector memory spec run and pass cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run spec: `venv\Scripts\pytest spec/vectormemoryspec.py`
- Validate that search skernels decay with time.
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/04-memory-extensibility-and-resiliency-harness/04-01-SUMMARY.md`
</output>
