---
phase: 04-memory-extensibility-and-resiliency-harness
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - kernel/plugins.py
  - spec/pluginregistryspec.py
autonomous: true
requirements:
  - EXT-03
  - EXT-04
must_haves:
  truths:
    - "The plugin registry downloader client successfully fetches and installs custom plugins to ops/addons/."
    - "The downloader client verifies package dependency versions and compatibility before installation."
  artifacts:
    - "kernel/plugins.py"
    - "spec/pluginregistryspec.py"
  key_links:
    - "Downloader client imports packaging.specifiers to validate version rules."
---

<objective>
Create kernel/plugins.py to implement a remote plugin registry downloader and dependency verifier, ensuring downloaded ops are version-compatible before installation.
</objective>

<execution_context>
@.agents/gsd-kernel/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 04-03-01: Create Plugin Registry Downloader Client</name>
  <files>kernel/plugins.py</files>
  <read_first></read_first>
  <action>
    - Create `kernel/plugins.py`.
    - Implement `PluginRegistryClient` class with methods `fetch_plugin_manifest(name: str, registry_url: str) -> dict` and `download_plugin(name: str, registry_url: str) -> str`.
    - Write downloaded files to `ops/addons/{name}.py` and register the new tool in the active registry.
  </action>
  <verify>Check kernel/plugins.py creation.</verify>
  <acceptance_criteria>
    - `kernel/plugins.py` is created with class definition.
  </acceptance_criteria>
  <done>Registry downloader client class is created.</done>
</task>

<task type="auto">
  <name>Task 04-03-02: Implement Dependency Version Resolution</name>
  <files>kernel/plugins.py</files>
  <read_first>kernel/plugins.py</read_first>
  <action>
    - In `PluginRegistryClient`, implement `verify_dependencies(manifest: dict) -> None`.
    - Retrieve installed package versions from the current python environment using `importlib.metadata`.
    - Parse manifest dependencies (e.g. `{"numpy": ">=1.20.0"}`) and use `packaging.specifiers.SpecifierSet` to check if installed version is compatible.
    - Raise `AgentError` with code `MISSING_DEPENDENCY` or `INCOMPATIBLE_VERSION` if verification fails.
  </action>
  <verify>Verify error propagation and package check imports.</verify>
  <acceptance_criteria>
    - Dependencies are correctly checked using specifier sets.
  </acceptance_criteria>
  <done>Dependency checks are implemented and raise correct errors on mismatches.</done>
</task>

<task type="auto">
  <name>Task 04-03-03: Create Downloader & Dependency Tests</name>
  <files>spec/pluginregistryspec.py</files>
  <read_first>kernel/plugins.py</read_first>
  <action>
    Create unit spec verifying:
    - Downloader fetches and parses plugin manifests.
    - Successful installation to `ops/addons/` directory.
    - Detection of missing dependency libraries.
    - Rejection of incompatible package versions.
    - Dynamic loading and registration of the downloaded tool.
  </action>
  <verify>Run `pytest spec/pluginregistryspec.py`</verify>
  <acceptance_criteria>
    - Downloader and dependency spec pass.
  </acceptance_criteria>
  <done>All registry and installer unit spec run and pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run spec: `venv\Scripts\pytest spec/pluginregistryspec.py`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/04-memory-extensibility-and-resiliency-harness/04-03-SUMMARY.md`
</output>
