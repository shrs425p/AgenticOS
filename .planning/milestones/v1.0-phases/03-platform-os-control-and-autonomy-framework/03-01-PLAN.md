---
phase: 03-platform-os-control-and-autonomy-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["ops/platform/__init__.py", "ops/platform/windows_ui.py", "ops/platform/macos_ui.py", "ops/platform/linux_desktop.py", "requirements.txt", "spec/test_platform_ui.py"]
autonomous: true
requirements: ["OS-01", "OS-02"]
must_haves:
  truths:
    - "pywin32 dependency conditionally declared in requirements.txt"
    - "Native Windows UI listing, window focus, click simulation, and WScript SendKeys typing implemented"
    - "Native macOS AppleScript processes listing, activate, menu clicks, keystroke typing, and Accessibility check/prompt implemented"
    - "Linux Wayland/X11 session detection and grim/scrot screenshots dispatch implemented"
    - "OS dispatcher in ops/platform/__init__.py correctly routes GUI ops"
  artifacts:
    - path: "ops/platform/__init__.py"
      provides: "Platform OS UI dispatching"
    - path: "ops/platform/windows_ui.py"
      provides: "Windows native UI listing and control using pywin32"
    - path: "ops/platform/macos_ui.py"
      provides: "macOS UI listing and control using Applescripts"
    - path: "ops/platform/linux_desktop.py"
      provides: "Linux session detection and screenshot utility"
    - path: "spec/test_platform_ui.py"
      provides: "Mock-based unit testing for all OS UI ops"
  key_links:
    - from: "spec/test_platform_ui.py"
      to: "ops/platform/windows_ui.py"
      via: "imports and spec Windows UI listing and click/type simulation"
    - from: "spec/test_platform_ui.py"
      to: "ops/platform/macos_ui.py"
      via: "imports and spec macOS subprocess calls and Accessibility permission prompts"
    - from: "spec/test_platform_ui.py"
      to: "ops/platform/linux_desktop.py"
      via: "imports and spec Linux session socket connections and grim/scrot screenshots"
---

# Plan 03-01: Native Platform UI Control

<objective>
Provide unified OS-level UI control (window listing, window focus, click, type, and screenshot dispatch) for Windows, macOS, and Linux, ensuring security boundary checks and proper user elevation warnings.
</objective>

<execution_context>
@.agents/gsd-kernel/workflows/execute-plan.md
@.agents/gsd-kernel/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-platform-os-control-and-autonomy-framework/03-CONTEXT.md
@.planning/phases/03-platform-os-control-and-autonomy-framework/03-RESEARCH.md
</context>

<threat_model>
## Trust Boundaries
- Subprocess execution boundary for running Applescript `osascript` on macOS, and `grim`/`scrot` screenshot utilities on Linux.
- Windows API security boundary when querying process elevation tokens or dispatching WScript.Shell COM calls.

## STRIDE Register
| ID | Threat | Component | Mitigation |
|----|--------|-----------|------------|
| T-03-01 | Elevation of Privilege | windows_ui.py | Detect if the targeted process is UAC-elevated; if so, raise AgentError advising the user to run the agent in admin mode rather than silently failing to control it. |
| T-03-02 | Tampering / Command Injection | macos_ui.py | Subprocess calls to `osascript` must escape user strings properly or construct Applescripts dynamically without format-injection vulnerabilities. |
| T-03-03 | Denial of Service / Hanging | macos_ui.py / linux_desktop.py | Impose 5-second timeouts on all GUI subprocess calls to prevent blocking the agent execution loop indefinitely if a GUI utility hangs. |
</threat_model>

## Artifacts this phase produces
- `ops/platform/__init__.py`: Cross-platform dispatcher routing GUI operations to appropriate OS backends.
- `ops/platform/windows_ui.py`: Native Windows GUI automation implementation using pywin32.
- `ops/platform/macos_ui.py`: macOS GUI automation using osascript (AppleScript).
- `ops/platform/linux_desktop.py`: Linux session detection and screenshots wrapper (grim/scrot).
- `spec/test_platform_ui.py`: Unit spec covering all platforms with mocks.

<tasks>
<task type="auto">
  <name>Task 1: Add pywin32 conditional dependency in requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>requirements.txt</read_first>
  <action>
    Add `pywin32>=306; sys_platform == 'win32'` to requirements.txt as a platform-conditional dependency.
  </action>
  <verify>Check requirements.txt contents to ensure pywin32 is appended under win32 platform check.</verify>
  <done>Conditional dependency is specified in requirements.txt.</done>
</task>

<task type="auto">
  <name>Task 2: Define AgentError class in kernel/errors.py if missing</name>
  <files>kernel/errors.py</files>
  <read_first>kernel/errors.py</read_first>
  <action>
    Ensure class `AgentError` exists in `kernel/errors.py`. If it does not exist, define it:
    ```python
    class AgentError(Exception):
        def __init__(self, message: str, code: str = "AGENT_ERR"):
            super().__init__(message)
            self.code = code
    ```
  </action>
  <verify>Inspect kernel/errors.py to ensure AgentError is defined.</verify>
  <done>AgentError class is defined and ready to be used.</done>
</task>

<task type="auto">
  <name>Task 3: Implement ops/platform/windows_ui.py ops</name>
  <files>ops/platform/windows_ui.py</files>
  <read_first>kernel/base.py, .planning/phases/03-platform-os-control-and-autonomy-framework/03-RESEARCH.md</read_first>
  <action>
    Create `ops/platform/windows_ui.py`. Import necessary pywin32 modules safely inside try-except block, falling back to pyautogui if missing.
    Implement the following ops using `@tool` decorator:
    - `list_windows() -> list[dict]`: Enumerates top-level visible windows via `win32gui.EnumWindows` and returns list of `{"hwnd": int, "title": str, "class": str}`.
    - `focus_window(hwnd: int) -> None`: Brings window to foreground via `SetForegroundWindow`. If iconic, call `ShowWindow` with SW_RESTORE first. Check process elevation via process token querying; if elevated and caller is not, raise `AgentError`.
    - `click_at(x: int, y: int) -> None`: Set cursor position via `win32api.SetCursorPos` and dispatch mouse down and up events via `win32api.mouse_event`.
    - `typetext(text: str) -> None`: Dispatch keys using COM WScript.Shell `SendKeys`, escaping special characters properly.
  </action>
  <verify>Verify code structures in windows_ui.py match the design guidelines. Run test setup to import it.</verify>
  <done>windows_ui.py is created, fully implemented without placeholders, and registers all ops.</done>
</task>

<task type="auto">
  <name>Task 4: Implement ops/platform/macos_ui.py ops</name>
  <files>ops/platform/macos_ui.py</files>
  <read_first>kernel/base.py, .planning/phases/03-platform-os-control-and-autonomy-framework/03-RESEARCH.md</read_first>
  <action>
    Create `ops/platform/macos_ui.py`. Implement ops with `@tool`:
    - `list_windows() -> list[str]`: Run Applescript `tell application "System Events" to get name of every process whose background only is false` via subprocess.
    - `focus_window(app_name: str) -> None`: Run Applescript `tell application app_name to activate`.
    - `click_menu_item(app_name: str, menu_item: str, menu_name: str) -> None`: Click menu item via applescript command.
    - `typetext(text: str) -> None`: Run Applescript `tell application "System Events" to keystroke text`.
    - Accessibility permission check: Implement `check_accessibility_permission() -> bool` which probes osascript. If it returns False, call `prompt_accessibility_permission()` to open preferences panel and raise `AgentError`.
  </action>
  <verify>Verify the code structures in macos_ui.py and ensure accessibility check handles missing permissions cleanly.</verify>
  <done>macos_ui.py is created with all macOS ops implemented and registered.</done>
</task>

<task type="auto">
  <name>Task 5: Implement ops/platform/linux_desktop.py ops</name>
  <files>ops/platform/linux_desktop.py</files>
  <read_first>kernel/base.py, .planning/phases/03-platform-os-control-and-autonomy-framework/03-RESEARCH.md</read_first>
  <action>
    Create `ops/platform/linux_desktop.py`. Implement ops with `@tool`:
    - `detect_linux_session() -> dict`: Reads `$XDG_SESSION_TYPE`, `$WAYLAND_DISPLAY`, `$DISPLAY`, `$XDG_CURRENT_DESKTOP`. Fallback socket connection check on `/run/user/{uid}/wayland-0` if env-vars are empty.
    - `takescreenshot(output_path: str, monitor: str = "") -> str`: Dispatches to `grim` if session is Wayland, or `scrot` if session is X11. Probes tool presence with `shutil.which`. If missing, raises `AgentError` with distro install commands.
  </action>
  <verify>Check linux_desktop.py for correct Wayland socket probe and shutil.which guards.</verify>
  <done>linux_desktop.py is created and fully implemented.</done>
</task>

<task type="auto">
  <name>Task 6: Wire up platform dispatcher tool init and platform API</name>
  <files>ops/platform/__init__.py, kernel/osio.py</files>
  <read_first>kernel/osio.py</read_first>
  <action>
    Create `ops/platform/__init__.py` to import correct platform UI functions depending on `platform.system()`.
    Extend `kernel/osio.py` by adding `get_ui_backend()` static method returning `ops.platform` package, keeping the rest of the existing stub unchanged.
  </action>
  <verify>Ensure `kernel/osio.py` resolves platform imports without crash on all operating systems.</verify>
  <done>Platform API dispatcher and ops package init are wired up.</done>
</task>

<task type="auto">
  <name>Task 7: Create spec/test_platform_ui.py unit spec</name>
  <files>spec/test_platform_ui.py</files>
  <read_first>spec/test_safety.py</read_first>
  <action>
    Create `spec/test_platform_ui.py` covering:
    - Windows: mock `win32gui`, `win32api`, `win32com`, `win32con`, `win32process`, `win32security` and verify window enumeration lists correctly.
    - macOS: mock subprocess.run for osascript process list and verify correct parser behavior; test accessibility fallback warning.
    - Linux: mock env vars for Wayland/X11, mock `shutil.which` and verify `takescreenshot` raises correctly when utility is missing.
  </action>
  <verify>Run `pytest spec/test_platform_ui.py` and confirm all spec pass.</verify>
  <done>All platform UI spec pass.</done>
</task>
</tasks>

<wave_summary>
| Wave | Tasks | Parallel? | Notes |
|------|-------|-----------|-------|
| 1 | Task 1 through Task 7 | Yes | All tasks in Plan 1 can be developed concurrently because they target isolated platform-specific files. |
</wave_summary>

<success_criteria>
- Native Windows pywin32 controls are active and detect elevated windows.
- Native macOS AppleScript controls are active and handle Accessibility prompts.
- Linux Wayland/X11 session detection works with socket-existence checks.
- Unified import routing is wired up.
- All test coverage passes successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/03-platform-os-control-and-autonomy-framework/03-01-SUMMARY.md`
</output>
