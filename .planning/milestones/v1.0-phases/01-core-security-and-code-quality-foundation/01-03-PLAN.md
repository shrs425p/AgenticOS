---
phase: 01-kernel-security-and-code-quality-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: ["kernel/schema.py", "kernel/cli.py", "kernel/base.py"]
autonomous: true
requirements: ["QUAL-01", "QUAL-02", "QUAL-03", "QUAL-04"]

must_haves:
  truths:
    - "Application cfg is parsed and validated using Pydantic models"
    - "Tool protocol defines interface for system and custom ops"
    - "Modular sub-modules in kernel/ runtime delegate execution without circular dependencies"
  artifacts:
    - path: "kernel/schema.py"
      provides: "Pydantic configuration validation schemas"
    - path: "kernel/base.py"
      provides: "Type-safe Tool protocol definition"
    - path: "kernel/agent.py"
      provides: "Modular agent runtime execution loop"
    - path: "kernel/dispatch.py"
      provides: "Modular action validator and dispatcher"
  key_links:
    - from: "kernel/cli.py"
      to: "kernel/agent.py"
      via: "delegates kernel session orchestration to the modular sub-module"
    - from: "kernel/registry.py"
      to: "kernel/base.py"
      via: "imports Tool metadata schema and protocol interface for typing ops"
---

<objective>
Implement type-safe configuration schemas, modularize monolithic runtime, and define standard tool protocols.

Purpose: Improve structural maintainability, validate cfg schemas dynamically, and eliminate circular imports during runtime setup.
Output: Pydantic schemas in kernel/schema.py, Tool protocol in kernel/base.py, refactored sub-modules in kernel/.
</objective>

<execution_context>
@.agents/gsd-kernel/workflows/execute-plan.md
@.agents/gsd-kernel/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-kernel-security-and-code-quality-foundation/01-CONTEXT.md
@.planning/phases/01-kernel-security-and-code-quality-foundation/01-RESEARCH.md
@.planning/phases/01-kernel-security-and-code-quality-foundation/01-VALIDATION.md
@kernel/cli.py
@kernel/schema.py
@kernel/base.py
</context>

<threat_model>
  <trust_boundaries>
    - System Configuration loading boundary.
    - Action and tool invocation type validation boundaries.
  </stride_register>
  <stride_register>
    - **Spoofing**: Insecure ops masquerading as system ops. Mitigated by registering ops under a strict Tool Protocol.
    - **Tampering**: Invalid or malformed configuration parameters crashing the agent or weakening guardrails. Mitigated by Pydantic schema validation at boot time.
    - **Repudiation**: N/A for this plan.
    - **Information Disclosure**: N/A for this plan.
    - **Denial of Service**: Circular imports causing runtime crashes during startup. Resolved by refactoring monolithic runtime.py and implementing dynamic tool registers.
    - **Elevation of Privilege**: N/A for this plan.
  </stride_register>
</threat_model>

## Artifacts this phase produces
- `kernel/schema.py`: refactored with Pydantic configuration schemas.
- `kernel/base.py`: updated with standard `Tool` protocol.
- `kernel/agent.py`: new module containing agent session loops.
- `kernel/dispatch.py`: new module handling action routing/validation.

<tasks>
<task type="auto">
  <name>Task 1: Define Pydantic cfg models in kernel/schema.py</name>
  <files>kernel/schema.py</files>
  <read_first>kernel/schema.py, .planning/phases/01-kernel-security-and-code-quality-foundation/01-RESEARCH.md</read_first>
  <action>
    Define Pydantic schema models corresponding to configuration parameters:
      - `NvidiaConfig` and `ProviderConfig` (validated LLM targets)
      - `CloudConfig` (all supported LLM backends)
      - `AgentConfig` (workspace, default models, max iterations)
      - `ConfigDict` (main entry cfg dictionary matching the active keys)
    Ensure configuration loading handles validation exceptions and falls back gracefully where needed.
  </action>
  <verify>Run `pytest spec/test_cfg.py`</verify>
  <done>Configuration types are migrated to Pydantic and verified through cfg unit spec.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Tool Protocol and register custom/system ops</name>
  <files>kernel/base.py</files>
  <read_first>kernel/base.py, .planning/phases/01-kernel-security-and-code-quality-foundation/01-RESEARCH.md</read_first>
  <action>
    Add `ToolMetadata` Pydantic model and `Tool` Protocol in `kernel/base.py` as outlined in research details.
    Verify `Tool` matches a callable protocol specifying name, description, category, version, and author metadata.
    Refactor `kernel/registry.py` to lazily import default subsystem modules inside `_register_all()` to eliminate circular import loops.
  </action>
  <verify>Run `pytest spec/toolregistryspec.py`</verify>
  <done>Tool protocol is active and tool registry resolves imports dynamically.</done>
</task>

<task type="auto">
  <name>Task 3: Modularize kernel/cli.py into kernel/agent.py and kernel/dispatch.py</name>
  <files>kernel/cli.py, kernel/agent.py, kernel/dispatch.py</files>
  <read_first>kernel/cli.py</read_first>
  <action>
    Refactor `kernel/cli.py` to extract orchestrator execution loop functions into `kernel/agent.py`.
    Extract action validation and execution dispatchers into `kernel/dispatch.py`.
    Provide import mapping inside `kernel/cli.py` so that existing client initializations and spec remain fully backward compatible.
  </action>
  <verify>Run `pytest spec/runtimespec.py`</verify>
  <done>Monolithic runtime.py is divided into distinct execution/dispatch modules, and existing spec pass.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest spec/test_cfg.py passes
- [ ] pytest spec/toolregistryspec.py passes
- [ ] pytest spec/runtimespec.py passes
</verification>

<success_criteria>
- All tasks completed.
- Pydantic validates incoming configuration structure.
- Tool protocol guarantees type-safety across filesystem/terminal subsystems.
- Circular imports are resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/01-kernel-security-and-code-quality-foundation/01-03-SUMMARY.md`
</output>
