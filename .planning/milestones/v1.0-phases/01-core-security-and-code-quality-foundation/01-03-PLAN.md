---
phase: 01-core-security-and-code-quality-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: ["core/config_types.py", "core/runtime.py", "core/tool_base.py"]
autonomous: true
requirements: ["QUAL-01", "QUAL-02", "QUAL-03", "QUAL-04"]

must_haves:
  truths:
    - "Application config is parsed and validated using Pydantic models"
    - "Tool protocol defines interface for system and custom tools"
    - "Modular sub-modules in core/ runtime delegate execution without circular dependencies"
  artifacts:
    - path: "core/config_types.py"
      provides: "Pydantic configuration validation schemas"
    - path: "core/tool_base.py"
      provides: "Type-safe Tool protocol definition"
    - path: "core/orchestrator.py"
      provides: "Modular agent runtime execution loop"
    - path: "core/dispatcher.py"
      provides: "Modular action validator and dispatcher"
  key_links:
    - from: "core/runtime.py"
      to: "core/orchestrator.py"
      via: "delegates core session orchestration to the modular sub-module"
    - from: "core/tool_registry.py"
      to: "core/tool_base.py"
      via: "imports Tool metadata schema and protocol interface for typing tools"
---

<objective>
Implement type-safe configuration schemas, modularize monolithic runtime, and define standard tool protocols.

Purpose: Improve structural maintainability, validate config schemas dynamically, and eliminate circular imports during runtime setup.
Output: Pydantic schemas in core/config_types.py, Tool protocol in core/tool_base.py, refactored sub-modules in core/.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-security-and-code-quality-foundation/01-CONTEXT.md
@.planning/phases/01-core-security-and-code-quality-foundation/01-RESEARCH.md
@.planning/phases/01-core-security-and-code-quality-foundation/01-VALIDATION.md
@core/runtime.py
@core/config_types.py
@core/tool_base.py
</context>

<threat_model>
  <trust_boundaries>
    - System Configuration loading boundary.
    - Action and tool invocation type validation boundaries.
  </stride_register>
  <stride_register>
    - **Spoofing**: Insecure tools masquerading as system tools. Mitigated by registering tools under a strict Tool Protocol.
    - **Tampering**: Invalid or malformed configuration parameters crashing the agent or weakening guardrails. Mitigated by Pydantic schema validation at boot time.
    - **Repudiation**: N/A for this plan.
    - **Information Disclosure**: N/A for this plan.
    - **Denial of Service**: Circular imports causing runtime crashes during startup. Resolved by refactoring monolithic runtime.py and implementing dynamic tool registers.
    - **Elevation of Privilege**: N/A for this plan.
  </stride_register>
</threat_model>

## Artifacts this phase produces
- `core/config_types.py`: refactored with Pydantic configuration schemas.
- `core/tool_base.py`: updated with standard `Tool` protocol.
- `core/orchestrator.py`: new module containing agent session loops.
- `core/dispatcher.py`: new module handling action routing/validation.

<tasks>
<task type="auto">
  <name>Task 1: Define Pydantic config models in core/config_types.py</name>
  <files>core/config_types.py</files>
  <read_first>core/config_types.py, .planning/phases/01-core-security-and-code-quality-foundation/01-RESEARCH.md</read_first>
  <action>
    Define Pydantic schema models corresponding to configuration parameters:
      - `NvidiaConfig` and `ProviderConfig` (validated LLM targets)
      - `CloudConfig` (all supported LLM backends)
      - `AgentConfig` (workspace, default models, max iterations)
      - `ConfigDict` (main entry config dictionary matching the active keys)
    Ensure configuration loading handles validation exceptions and falls back gracefully where needed.
  </action>
  <verify>Run `pytest tests/test_config.py`</verify>
  <done>Configuration types are migrated to Pydantic and verified through config unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Tool Protocol and register custom/system tools</name>
  <files>core/tool_base.py</files>
  <read_first>core/tool_base.py, .planning/phases/01-core-security-and-code-quality-foundation/01-RESEARCH.md</read_first>
  <action>
    Add `ToolMetadata` Pydantic model and `Tool` Protocol in `core/tool_base.py` as outlined in research details.
    Verify `Tool` matches a callable protocol specifying name, description, category, version, and author metadata.
    Refactor `core/tool_registry.py` to lazily import default subsystem modules inside `_register_all()` to eliminate circular import loops.
  </action>
  <verify>Run `pytest tests/test_tool_registry.py`</verify>
  <done>Tool protocol is active and tool registry resolves imports dynamically.</done>
</task>

<task type="auto">
  <name>Task 3: Modularize core/runtime.py into core/orchestrator.py and core/dispatcher.py</name>
  <files>core/runtime.py, core/orchestrator.py, core/dispatcher.py</files>
  <read_first>core/runtime.py</read_first>
  <action>
    Refactor `core/runtime.py` to extract orchestrator execution loop functions into `core/orchestrator.py`.
    Extract action validation and execution dispatchers into `core/dispatcher.py`.
    Provide import mapping inside `core/runtime.py` so that existing client initializations and tests remain fully backward compatible.
  </action>
  <verify>Run `pytest tests/test_runtime.py`</verify>
  <done>Monolithic runtime.py is divided into distinct execution/dispatch modules, and existing tests pass.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest tests/test_config.py passes
- [ ] pytest tests/test_tool_registry.py passes
- [ ] pytest tests/test_runtime.py passes
</verification>

<success_criteria>
- All tasks completed.
- Pydantic validates incoming configuration structure.
- Tool protocol guarantees type-safety across filesystem/terminal subsystems.
- Circular imports are resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-security-and-code-quality-foundation/01-03-SUMMARY.md`
</output>
